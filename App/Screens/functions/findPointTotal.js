import React, { useState } from "react";

import { FlatList, Text, View, Button, TextInput, Picker } from "react-native";
import { f, database, auth, storage } from "../config/config";

export default function findPointTotal(userID) {}
